- content_for :timeline_heading do
  #timeline_heading
    %h1 Retweets By You

- content_for :pagination do
  = pagination :newer => retweets_path(:page => @newer_page), :older => retweets_path(:page => @older_page)

= render :partial => 'update'
= render :partial => 'timeline'
